<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodel="clr-namespace:GifsApp.ViewModel"
             x:Class="GifsApp.MainPage">

    <ContentPage.BindingContext>
        <viewmodel:MainPageViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout Margin="10">

            <SearchBar Placeholder="Search your gift"
                       Text="{Binding SearchText}"
                       SearchCommand="{Binding }"
                       SearchCommandParameter="{Binding .}"/>

            <ScrollView>
                <Label Text="Hi here!"/>
            </ScrollView>

        </StackLayout>

        
    </ContentPage.Content>

</ContentPage>
